language: c

compiler:
  - clang
  - gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq php5-dev libsmbclient-dev

script:
  - phpize
  - ./configure
  - make
  - sudo make install
